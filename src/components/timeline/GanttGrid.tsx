import type { Phase,Milestone } from '@/features/roadmap/roadmap.types'
import { pct } from '@/lib/format'
export function GanttGrid({phases,milestones}:{phases:Phase[];milestones:Milestone[]}){return <div className="overflow-x-auto rounded-2xl border border-border bg-card"><div className="min-w-[960px] p-4"><div className="grid grid-cols-[240px_repeat(12,minmax(46px,1fr))] gap-1 pb-3 text-center text-xs text-muted-foreground"><div className="text-right font-bold text-foreground">المسار</div>{Array.from({length:12},(_,i)=><div key={i} className={`rounded-lg py-2 ${i>=10?'bg-warning/10 text-warning':'bg-secondary'}`}>{i+1}</div>)}</div>{phases.map(p=><div key={p.id} className="grid grid-cols-[240px_repeat(12,minmax(46px,1fr))] gap-1 border-t border-border/60 py-2"><div className="pl-4"><div className="flex items-center justify-between gap-2"><span className="truncate text-sm font-bold">{p.name}</span><span className="text-xs text-muted-foreground">{pct(p.progress)}</span></div></div>{Array.from({length:12},(_,i)=>{const w=i+1,active=w>=p.start_week&&w<=p.end_week; return <div key={w} title={`${p.name} — الأسبوع ${w}`} className={`h-9 ${active?'bg-primary/90':i>=10?'bg-warning/5':'bg-secondary/35'} ${w===p.start_week?'rounded-r-lg':''} ${w===p.end_week?'rounded-l-lg':''}`}/>})}</div>)}<div className="mt-4 flex flex-wrap gap-2 border-t pt-4">{milestones.map(m=><span key={m.id} className="rounded-full border bg-background px-3 py-1 text-xs"><b>{m.title}</b> · أسبوع {m.week}</span>)}</div></div></div>}
