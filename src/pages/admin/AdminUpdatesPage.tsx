import { useState } from 'react'
import { Send } from 'lucide-react'
import { AdminPageHeader } from '@/components/admin/AdminPageHeader'
import { useRoadmap } from '@/features/roadmap/roadmap.queries'
import { useRoadmapMutations } from '@/features/roadmap/roadmap.mutations'
export default function AdminUpdatesPage(){const {data}=useRoadmap();const {update}=useRoadmapMutations();const [title,setTitle]=useState('');const [body,setBody]=useState('');const submit=(e:React.FormEvent)=>{e.preventDefault();if(!title.trim()||!body.trim())return;update.mutate({title:title.trim(),body:body.trim()},{onSuccess:()=>{setTitle('');setBody('')}})};return <><AdminPageHeader title="التحديثات" description="انشر تحديثًا مختصرًا وشفافًا للعميل."/><form onSubmit={submit} className="surface mb-6 rounded-2xl p-5"><div className="grid gap-4"><label className="text-sm font-bold">عنوان التحديث<input value={title} onChange={e=>setTitle(e.target.value)} className="mt-2 min-h-11 w-full rounded-xl border bg-background px-3" placeholder="مثال: اكتمال نموذج Alpha"/></label><label className="text-sm font-bold">التفاصيل<textarea value={body} onChange={e=>setBody(e.target.value)} className="mt-2 min-h-28 w-full rounded-xl border bg-background p-3" placeholder="ماذا تم؟ وما الخطوة التالية؟"/></label><button disabled={update.isPending} className="inline-flex min-h-11 w-fit items-center gap-2 rounded-xl bg-primary px-4 font-bold text-primary-foreground"><Send className="h-4 w-4"/>نشر للعميل</button></div></form><div className="space-y-3">{data?.updates.map(u=><article key={u.id} className="surface rounded-2xl p-4"><span className="text-xs text-muted-foreground">{new Date(u.created_at).toLocaleString('ar-SA')}</span><h2 className="mt-1 font-black">{u.title}</h2><p className="mt-1 text-sm text-muted-foreground">{u.body}</p></article>)}</div></>}