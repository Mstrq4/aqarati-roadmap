import { useState } from 'react'
import { useNavigate,Link } from 'react-router-dom'
import { LockKeyhole,ArrowLeft,ShieldCheck } from 'lucide-react'
import { supabase,supabaseConfigured } from '@/lib/supabase'
import { ThemeToggle } from '@/components/app-shell/ThemeToggle'
export default function LoginPage(){const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [busy,setBusy]=useState(false);const nav=useNavigate();const submit=async(e:React.FormEvent)=>{e.preventDefault();if(!supabase){setError('لم يتم ربط Supabase بعد.');return}setBusy(true);setError('');const {error}=await supabase.auth.signInWithPassword({email,password});setBusy(false);if(error){setError('تعذر تسجيل الدخول. تحقق من البريد وكلمة المرور.');return}nav('/admin')};return <div className="min-h-screen bg-background p-4"><div className="mx-auto flex max-w-6xl justify-between py-4"><Link to="/" className="inline-flex min-h-11 items-center gap-2 rounded-xl px-2 text-sm font-bold"><ArrowLeft className="h-4 w-4"/>العودة للعرض</Link><ThemeToggle/></div><div className="mx-auto grid min-h-[78vh] max-w-6xl items-center gap-8 lg:grid-cols-[1.1fr_.9fr]"><section className="hidden rounded-[32px] bg-primary p-10 text-primary-foreground lg:block"><ShieldCheck className="h-10 w-10"/><h1 className="mt-10 text-5xl font-black leading-tight">لوحة تحكمك<br/><span className="font-light opacity-75">تبقى خاصة بك.</span></h1><p className="mt-5 max-w-md leading-8 opacity-75">عدّل نسب الإنجاز، الحالات، الدفعات والتحديثات. العميل يرى النتيجة فقط من رابط العرض العام.</p></section><form onSubmit={submit} className="surface rounded-3xl p-6 sm:p-8"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-secondary text-primary"><LockKeyhole className="h-6 w-6"/></span><h2 className="mt-5 text-3xl font-black">دخول الإدارة</h2><p className="mt-2 text-sm text-muted-foreground">حساب واحد خاص بصاحب المشروع.</p>{!supabaseConfigured&&<div className="mt-5 rounded-xl border border-warning/20 bg-warning/10 p-3 text-sm text-warning">بيئة Supabase لم تُضبط بعد؛ سيعمل تسجيل الدخول فور إضافة متغيرات البيئة.</div>}<label className="mt-6 block text-sm font-bold">البريد الإلكتروني<input required type="email" value={email} onChange={e=>setEmail(e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border bg-background px-4 outline-none focus:border-primary" autoComplete="email"/></label><label className="mt-4 block text-sm font-bold">كلمة المرور<input required type="password" value={password} onChange={e=>setPassword(e.target.value)} className="mt-2 min-h-12 w-full rounded-xl border bg-background px-4 outline-none focus:border-primary" autoComplete="current-password"/></label>{error&&<p className="mt-4 text-sm text-destructive">{error}</p>}<button disabled={busy} className="mt-6 min-h-12 w-full rounded-xl bg-primary px-4 font-black text-primary-foreground disabled:opacity-60">{busy?'جارٍ الدخول…':'تسجيل الدخول'}</button></form></div></div>}