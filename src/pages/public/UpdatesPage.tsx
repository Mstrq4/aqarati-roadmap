import { History } from 'lucide-react'
import { useRoadmap } from '@/features/roadmap/roadmap.queries'
export default function UpdatesPage(){const {data}=useRoadmap();if(!data)return null;return <div className="page-shell py-8 sm:py-10"><header className="mb-7"><span className="flex items-center gap-2 text-sm font-bold text-primary"><History className="h-5 w-5"/>سجل شفاف للعميل</span><h1 className="mt-2 text-4xl font-black sm:text-5xl">التحديثات</h1><p className="mt-3 text-muted-foreground">كل تحديث منشور يظهر هنا بترتيب زمني من الأحدث إلى الأقدم.</p></header><div className="relative max-w-4xl before:absolute before:bottom-0 before:right-[11px] before:top-2 before:w-px before:bg-border">{data.updates.map(u=><article key={u.id} className="relative mb-5 pr-9"><span className="absolute right-0 top-2 h-[23px] w-[23px] rounded-full border-4 border-background bg-primary"/><div className="surface rounded-2xl p-5"><time className="text-xs text-muted-foreground">{new Intl.DateTimeFormat('ar-SA',{dateStyle:'full'}).format(new Date(u.created_at))}</time><h2 className="mt-1 text-xl font-black">{u.title}</h2><p className="mt-2 leading-7 text-muted-foreground">{u.body}</p></div></article>)}</div></div>}
