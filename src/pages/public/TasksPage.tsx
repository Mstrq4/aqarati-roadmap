import { Search,ListChecks } from 'lucide-react'
import { useMemo,useState } from 'react'
import { useRoadmap } from '@/features/roadmap/roadmap.queries'
import { TaskTable,TaskCards } from '@/components/tasks/TaskTable'
export default function TasksPage(){const {data}=useRoadmap();const [q,setQ]=useState('');const [status,setStatus]=useState('all');const filtered=useMemo(()=>data?.tasks.filter(t=>(!q||t.title.includes(q))&&(status==='all'||t.status===status))??[],[data,q,status]);if(!data)return null;return <div className="page-shell py-8 sm:py-10"><header className="mb-7"><span className="flex items-center gap-2 text-sm font-bold text-primary"><ListChecks className="h-5 w-5"/>متابعة التنفيذ</span><h1 className="mt-2 text-4xl font-black sm:text-5xl">المهام</h1><p className="mt-3 text-muted-foreground">تفصيل العمل المخطط والمنفذ، مع الحالة ونسبة التقدم لكل مهمة.</p></header><div className="mb-5 grid gap-3 sm:grid-cols-[1fr_200px]"><label className="relative"><Search className="absolute right-4 top-1/2 h-4 w-4 -translate-y-1/2 text-muted-foreground"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="ابحث في المهام..." className="min-h-12 w-full rounded-xl border bg-card pr-11 pl-4 outline-none focus:border-primary"/></label><select value={status} onChange={e=>setStatus(e.target.value)} className="min-h-12 rounded-xl border bg-card px-4"><option value="all">كل الحالات</option><option value="planned">مخططة</option><option value="active">قيد التنفيذ</option><option value="review">مراجعة</option><option value="done">مكتملة</option><option value="blocked">متوقفة</option></select></div><div className="mb-3 text-sm text-muted-foreground">{filtered.length} مهمة</div><TaskTable tasks={filtered} phases={data.phases}/><TaskCards tasks={filtered} phases={data.phases}/></div>}
