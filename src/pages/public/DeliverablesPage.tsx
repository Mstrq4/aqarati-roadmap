import { FileCheck2,CheckCircle2,Circle } from 'lucide-react'
import { useRoadmap } from '@/features/roadmap/roadmap.queries'
const groups={scope:'اعتماد النطاق',alpha:'بوابة Alpha — الأسبوع 5',rc:'Release Candidate — الأسبوع 10',final:'التسليم النهائي'} as const
export default function DeliverablesPage(){const {data}=useRoadmap();if(!data)return null;return <div className="page-shell py-8 sm:py-10"><header className="mb-7"><span className="flex items-center gap-2 text-sm font-bold text-primary"><FileCheck2 className="h-5 w-5"/>معايير القبول</span><h1 className="mt-2 text-4xl font-black sm:text-5xl">التسليمات</h1><p className="mt-3 text-muted-foreground">ما الذي يجب أن يكون جاهزًا عند كل بوابة قبل اعتبارها مقبولة.</p></header><div className="grid gap-5 lg:grid-cols-2">{Object.entries(groups).map(([key,label])=><section key={key} className="surface rounded-3xl p-5 sm:p-6"><h2 className="text-xl font-black">{label}</h2><div className="mt-5 space-y-3">{data.deliverables.filter(d=>d.group_key===key).map(d=><div key={d.id} className="flex items-start gap-3 rounded-xl bg-secondary/40 p-3">{d.is_complete?<CheckCircle2 className="mt-0.5 h-5 w-5 shrink-0 text-success"/>:<Circle className="mt-0.5 h-5 w-5 shrink-0 text-muted-foreground"/>}<span className={d.is_complete?'font-bold':'text-muted-foreground'}>{d.title}</span></div>)}</div></section>)}</div></div>}
